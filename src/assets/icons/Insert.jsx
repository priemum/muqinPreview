import iconImg from "./solar_plus-minus-bold-duotone.png";
const InsertIcon = ({ width = 24, height = 24, rotate, ...props }) => {
  return <img alt="delete icon" src={iconImg} width={width} height={height} />;
};

export default InsertIcon;
